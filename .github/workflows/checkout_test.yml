name: test checkout

on:
  workflow_dispatch:
    inputs:
      command:
        description: 'checkout'
        required: true
        default: 'main'

jobs:
  run-command:
    runs-on: [ubuntu-latest] 

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: 📤 Show Output in Summary
        run: cat /home/runner/work/github_actions/github_actions/.git/config
